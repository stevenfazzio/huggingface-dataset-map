# Required
HF_TOKEN=               # HuggingFace token (read-only is fine; raises rate limits)
CO_API_KEY=             # Cohere API key (card embeddings)
ANTHROPIC_API_KEY=      # Anthropic API key (Toponymy topic naming via Claude Sonnet)
